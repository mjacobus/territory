<h1>Territories</h1>

<%= link_to('New', new_territory_path, class: 'btn btn-info') %>


<table class="table table-striped">
  <% @territories.each do |territory| %>
    <tr>
      <td> <%= territory.name %> </td>
      <td>
        <%= link_to 'Editar', edit_territory_path(territory) %> |
        <%= link_to 'Excluir', territory_path(territory), data: { method: :delete, confirm: 'Sure?' } %>
      </td>
    </tr>
  <% end %>
</table>
