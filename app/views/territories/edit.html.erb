<%= render partial: '/breadcrumb', locals: {
  items: [
    ['Territórios', territories_path],
    [@territory.name],
    ['Editar']
  ]
}
%>

<%= render partial: 'form', locals: { territory: @territory } %>
