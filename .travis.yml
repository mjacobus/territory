language: ruby
dist: trusty
cache: bundler
rvm:
  - 2.6

before_script:
  - gem install bundler:2.0.2
  - bundle install
  - bundle exec rake db:create
  - bundle exec rake db:migrate

script:
  - bundle exec rspec

services:
  - mysql
