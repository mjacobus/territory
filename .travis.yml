language: ruby
dist: trusty
cache: bundler
rvm:
  - 2.6

script:
  - gem install bundler
  - bundle install
  - bundle exec rake db:create
  - bundle exec rake db:migrate
  - bundle exec rspec

services:
  - mysql
